# 뉴스 요약에 쓰는 OpenAI API 키.
# 이 파일을 .env 로 복사한 뒤 실제 키를 넣는다. .env 는 커밋되지 않는다.
#
# 키가 없어도 `npm run ingest` 는 정상 종료한다 — 요약만 비워 두고 제목·링크는 저장한다.
# 나중에 키를 넣고 다시 돌리면 최근 3일치의 빈 요약을 채운다.
OPENAI_API_KEY=

# 요약에 쓸 모델. 비워 두면 gpt-5.6-luna.
OPENAI_MODEL=gpt-5.6-luna
